# Audit Log — `valuation-calc` vs `masterprompt2.md` (Parts 0–7)

Audit date: 2026-07-07. Master prompt: `masterprompt2.md` (identical in content to `MASTER_PROMPT.md`; the two differ only by blank lines).

Legend for Current Status: **Missing** = not present at all; **Wrong** = present but does not match the requirement; **Partial** = present, some of the requirement met; **OK** = compliant.

| Part # | Requirement | Current Status | File(s) Affected | Suggested Fix |
|---|---|---|---|---|
| 0 | `README.md` at repo root documenting the two-way JSON contract | Missing | `README.md` (absent) | Create `README.md` documenting the `ExtractionResult` → adapter → `FinancialStatements` contract and the `MarketDataProvider` interface. |
| 0 | `DEPLOY.md` at repo root (GitHub Pages plan, Part 5) | Missing | `DEPLOY.md` (absent) | Create `DEPLOY.md` per Part 5 (both deployment options; Vite `base` guidance). |
| 0 | `.gitignore` with `node_modules`, `dist`, `.env`, `*.local` | Missing | `.gitignore` (absent); `packages/contract/tsconfig.tsbuildinfo`, `packages/valuation-creator/core/tsconfig.tsbuildinfo` are stray build artifacts | Create `.gitignore` (also add `*.tsbuildinfo`); delete the stray `tsconfig.tsbuildinfo` files. |
| 0 | Repo is a git repository with the initial commit (Part 7 item 5) | Missing | repo root (no `.git/`) | `git init`, commit, add remote `https://github.com/keith2929/valuation-bot.git`, push `main`; stop and report on auth failure. |
| 0 | `packages/valuation-creator/src/providers/{FixtureProvider.ts, ExtractionProvider.ts}` | Wrong / Missing | `packages/valuation-creator/app/src/fixtures/FixtureProvider.ts` (wrong location + implements the wrong provider interface); `ExtractionProvider.ts` absent | Create `packages/valuation-creator/src/providers/`; move & rewrite `FixtureProvider` against the Part 4 `MarketDataProvider`; add `ExtractionProvider` (Part 3). |
| 0 | Dependency rule enforced via lint/import boundaries; `core` imports `contract` ONLY | Missing / Wrong | no ESLint config anywhere; `packages/valuation-creator/core/src/*` imports `contract` nowhere (defines its own local types instead of consuming the shared contract); `core/__tests__/adapterRoundTrip.test.ts` imports the adapter via relative path `../../../adapter/src/...` (crosses package boundary) | Add ESLint with import-boundary rules; make `core` consume `@valuation-bot/contract` types (`FinancialStatements`, `MarketData`, `PeerData`); move the adapter round-trip test into `packages/adapter`. |
| 0 | Bot 1 `src/` contains page finder, LLM call, JSON parse, tie-out validation | Partial | `packages/financial-statement-extract/src/` has only `parser.ts` and `tieOut.ts`; no page finder, no LLM-call module, no `index.ts`; `package.json` has no `build`/`test` scripts | Add `pageFinder.ts` (search text for "Statement of Financial Position", "Profit or Loss", "Cash Flows"), `extract.ts` (temperature-0 LLM call, JSON mode), `reviewQueue.ts` routing, `index.ts`; add `build`/`test` scripts. |
| 0 | Root workspaces configuration | Partial | `package.json` lists `"apps/*"` in `workspaces` (directory does not exist); `pnpm-workspace.yaml` is correct | Remove `"apps/*"` from `package.json` `workspaces` (or align it with `pnpm-workspace.yaml`). |
| 1 | Extraction prompt canonical tags — balance sheet | Wrong | `packages/financial-statement-extract/EXTRACTION_PROMPT.md` | Add missing balance-sheet tags: `short_term_investments`, `prepaid_expenses`, `long_term_investments`, `accrued_expenses`, `deferred_revenue_current`, `other_current_liabilities`, `deferred_revenue_non_current`, `pension_opeb`, `other_non_current_liabilities`. |
| 1 | Extraction prompt canonical tags — cash flow (`depreciation_amortization`, `capex_purchase_of_ppe`, `proceeds_from_disposal_of_ppe`, `dividends_paid` are required by the DCF/adapter) | Wrong | `packages/financial-statement-extract/EXTRACTION_PROMPT.md`; knock-on: `packages/adapter/src/toFinancialStatements.ts:243` hardcodes `capitalExpenditure = null` "no dedicated cash-flow capex tag" | Add the four missing cash-flow tags to the prompt (Part 1 tag list verbatim); then map them in the adapter. |
| 1 | Route tie-out mismatches / non-empty `extraction_warnings` to a review queue rather than the clean table | Missing | `packages/financial-statement-extract/src/` (no review-queue module; `tieOut.ts` returns violations but nothing consumes them) | Implement pipeline step: parse → validate vs `ExtractionResult` → `tieOut()` → route to review queue on violations/warnings, else store. |
| 1 | Tie-out validation in code (`total_assets == current + non-current`; `assets == liabilities + equity`) | OK | `packages/financial-statement-extract/src/tieOut.ts` | None (covers required checks plus extras). |
| 2.1 | Company search by ticker with exchange filter, resolving `CompanyRef` (name + reporting currency) | Missing | `packages/valuation-creator/app/src/App.tsx` (hardcodes `SIA_COUNTER_CODE = "C6L"`); contract lacks `searchTicker`/`CompanyRef` | Add search UI calling `MarketDataProvider.searchTicker(ticker, exchange)`; display name + reporting currency. Depends on the Part 4 contract fix. |
| 2.2 | ≥5 years historicals shown, 5-year forecast with COGS/SG&A/other-opex as % of revenue (scenario-driven) | Wrong | `packages/valuation-creator/core/src/forecast.ts` (drives EBITDA margin / D&A % / capex % / NWC % of revenue instead); no statements view in `App.tsx` | Rework the forecast layer to Part 2.2 drivers: revenue growth per year, COGS/SG&A/otherOpEx as % of revenue, capex as direct per-year inputs; render historical + forecast statements in the UI. |
| 2.2 | Working capital forecast on a days-outstanding basis; non-operating BS items held flat | Missing | `packages/valuation-creator/core/src/forecast.ts` (NWC modelled as a single % of revenue) | Implement days-outstanding working-capital schedules (receivables/inventory off revenue or COGS; payables, accrued, unearned revenue, prepaid, other current likewise); hold non-operating items flat; calibrate to Part 6 drivers and document the convention (§6.8 Tier 2). |
| 2.2 | Scenario toggle Bear / Base / Bull with editable offsets (~±1%) | Missing | `packages/valuation-creator/core/src/forecast.ts`, `App.tsx` | Add scenario type to the forecast input; wire the Part 6.3 `SIA_ASSUMPTIONS` bear/base/bull structures; expose offsets as editable inputs. |
| 2.2 | Recompute EBIT, EBITDA, EBT, tax, net income, balanced balance sheet + cash flow forward | Partial | `packages/valuation-creator/core/src/forecast.ts` (EBIT/EBITDA/NOPAT only; no EBT/net income, no forecast balance sheet or cash flow) | Extend the forecast to EBT (with interest), tax, net income, and a balanced balance sheet + cash flow forward. |
| 2.3 | Hamada peer inputs: `assetBeta = equityBeta5Y / (1 + (totalDebt/equityValue)*(1−tax))`, peer D/E from `totalDebt` and market `equityValue` (converted via `fxToTargetCurrency`); allow excluding outliers | Partial | `packages/valuation-creator/core/src/hamada.ts` (math correct but takes a precomputed `debtToEquity`; no `totalDebt`/`equityValue`/fx inputs; no outlier-exclusion mechanism) | Accept `PeerData`-shaped peers (totalDebt, equityValue, fx, marginalTaxRate); compute D/E internally; add an `exclude` flag / exclusion rule so the SIA average of 0.4805 is reproducible. |
| 2.4 | Cost of debt: `preTaxKd = interestExpense / marketValueOfDebt` | Partial | `packages/valuation-creator/core/src/wacc.ts` (accepts only a direct `preTaxCostOfDebt`) | Add optional derivation from `interestExpense` / `marketValueOfDebt`, keeping the direct override (fixture supplies 0.041). |
| 2.5 | DCF: PV of TV uses the final-period discount factor (mid-year, N−0.5) for BOTH methods — reference: exit TV 23385.89 → PV 17918.93 = ÷1.061^4.5 | Wrong | `packages/valuation-creator/core/src/dcf.ts:265` (exit-multiple TV discounted over `horizon` = N periods); same convention duplicated in `sensitivity.ts:335` and `footballField.ts:220` | Discount the exit-multiple TV with the final-period mid-year factor (N−0.5), matching Gordon; update the three call sites and doc comments. |
| 2.5 | DCF bridge `EV − totalDebt + cash = equity`; implied price; upside/(downside) vs current price | Partial | `packages/valuation-creator/core/src/dcf.ts` (netDebt-based bridge is arithmetically equivalent, but upside vs current price is not computed anywhere) | Add current price to the DCF result path and report implied price + upside/(downside). |
| 2.5 | Exit multiple = comps **median** LTM EV/EBITDA | Missing | no comps module exists to source the multiple; `App.tsx` hardcodes `exitMultiple: 8` | Implement comps (below) and default the DCF exit multiple to the comps median LTM EV/EBITDA (4.7454x for SIA). |
| 2.5 | Comparable companies model: LTM & NTM P/E, P/B, EV/EBITDA; peer figures converted via `fxToTargetCurrency`; Avg/Min/25th/Median/75th/Max; peer `EV = marketCap + (−cash + totalDebt + preferred + minorityInterest)*fx`; implied prices | Missing | no `comps.ts` in `packages/valuation-creator/core/src/` | Create `core/src/comps.ts` implementing the full comps engine per Part 2.5 and unit-test against §6.7 comps reference ranges. |
| 2.5 | DDM: PV dividends at cost of equity, GG terminal value at Ke, payout = avg of last 2 years, editable special-DPS schedule → implied price **3.8674** for SIA | Partial | `packages/valuation-creator/core/src/ddm.ts` (mechanics present, per-share; year-end discounting unverified against the reference); `core/__tests__/fixtures/siaValuation.ts:94` pins DDM to **5.9055**, which is actually the DCF exit-multiple implied price in §6.7 | Validate the DDM against the §6.7 reference (PV div 2691.10 + PV TV 9498.67 → $3.8674) with Part 6 inputs; calibrate the discounting convention to reproduce it; fix the mislabelled golden value. |
| 2.6 | Sensitivity grids: DCF Gordon (WACC × TGR), DCF exit (WACC × terminal EBITDA margin, plus WACC × exit multiple as an option), DDM (COE × TGR); default steps WACC/COE ±1%, margin ±1%, growth ±0.5%, step size editable; heatmap tables | Partial | `packages/valuation-creator/core/src/sensitivity.ts` (has WACC×TGR and WACC×EBITDA-margin; no DDM COE×TGR grid; no WACC×exit-multiple grid); `App.tsx:104-125` hardcodes steps 0.005/0.0025/0.025 and renders line charts, not heatmap tables | Add `ddmSensitivityGrid` (COE × TGR) and an optional WACC × exit-multiple grid; use spec default steps (±1%/±1%/±0.5%) with user-editable step size; render as heatmap tables. Axes per §6.6. |
| 2.7 | Football field: 7 bars (DCF Gordon point, DCF exit point, LTM EV/EBITDA, LTM P/B, NTM EV/EBITDA, DDM point, 52-week high/low) overlaying current price and target price (= DCF exit implied) | Wrong | `packages/valuation-creator/core/src/footballField.ts` (3 scenario-spread bars only: DCF GG, DCF exit, DDM; no comps bars, no 52-week bar, no price overlays) | Rework football field to the 7 spec bars with Min/Mean/Max from comps ranges, point values for DCF/DDM, 52-week from `MarketData`, and current/target price overlays (§6.7 values). |
| 2.8 | Sign-convention unit test (sign-flipped feed caught by a test); provider response caching per company/session; calls scoped by exchange | Missing | no such test in `packages/valuation-creator/core/__tests__/`; no caching layer; no exchange scoping (blocked on Part 4 contract) | Add a sign-flip unit test on the FCFF/CF assembly; add per-company/session provider cache; scope provider calls by exchange. |
| 2 | Every assumption is a user-editable input in one labelled assumptions panel — never hardcoded | Wrong | `packages/valuation-creator/app/src/App.tsx:44-57` (`ASSUMPTIONS` const with placeholder values: Rf 0.03, MRP 0.05, exit 8x, netDebt 0, etc.) | Build an assumptions panel (blue-input convention) seeded from `SIA_ASSUMPTIONS`; remove all hardcoded assumption values. |
| 3 | Adapter signature: `toFinancialStatements(reports: ExtractionResult[]) → { statements: FinancialStatements; warnings: string[] }` | Wrong | `packages/adapter/src/toFinancialStatements.ts:29-31` (returns `FinancialStatements[]` of per-period flat objects) | Rewrite to the Part 3 signature, producing the Part 4 multi-year `FinancialStatements` plus a `warnings` array. |
| 3a | Stitch ≥5 fiscal years; on overlap cross-check earlier-current vs later-comparative, prefer audited **and warn** on mismatch | Partial | `packages/adapter/src/toFinancialStatements.ts` (stitches and ranks audited-first, but silently — no mismatch warning; output is not the ≥5-year `fiscalYears` timeline shape) | Emit a warning on cross-check mismatch; assemble the merged timeline into `fiscalYears` + parallel arrays. |
| 3b | Assert `unit_multiplier` consistency and per-report `currency` equality (warn, don't convert) | Missing | `packages/adapter/src/toFinancialStatements.ts` (carries the fields through without checks) | Add both consistency checks, appending to `warnings`. |
| 3c | Map tags → canonical fields per the Part 3 table (e.g. `sga` = `administrative_expenses`+`distribution_costs`, `netPPE` = `ppe`(+ROU, decide & document), `commonEquity` = `share_capital`+`reserves`−`treasury_shares`, `accrued` = `accrued_expenses`/`other_payables`, unmapped required field → 0 + warning) | Missing | `packages/adapter/src/toFinancialStatements.ts` (maps to the old flat shape; none of the ~40 canonical fields of Part 4 exist) | Implement the full Part 3 mapping table onto the Part 4 `FinancialStatements`, with 0 + warning for unmapped required fields. |
| 3d | Derive WC deltas: `changeReceivables`, `changeInventory`, `changePayables`, `changeUnearnedRev`, `changeOtherNWC` as YoY balance-sheet differences | Partial | `packages/adapter/src/toFinancialStatements.ts:296-303` (derives only `change_receivables` and an aggregate `change_nwc`) | Derive all five contract deltas from the mapped balance-sheet items (not the cash flow). |
| 3e | Re-run tie-outs on the assembled result; forward every failure + every input `extraction_warning` into `warnings`; UI flags warnings | Missing | `packages/adapter/src/toFinancialStatements.ts` (input `extraction_warnings` are dropped; no tie-out re-run); no UI flagging | Re-run tie-outs post-assembly; concatenate all input warnings; surface warnings in the app. |
| 3 | `ExtractionProvider`: `getFinancials()` = stored `ExtractionResult[]` → adapter; `getMarketData()`/`getPeer()` clearly stubbed on fixture | Missing | `packages/valuation-creator/src/providers/ExtractionProvider.ts` (absent) | Implement per Part 3, with market data/peers stubbed and marked for the future live API. |
| 4 | `contract/financials.ts`: `Exchange`, `CompanyRef`, multi-year `FinancialStatements` (fiscalYears + incomeStatement/balanceSheet/cashFlow arrays), `MarketData`, `PeerData`, `MarketDataProvider { searchTicker, getFinancials, getMarketData, getPeer }` | Wrong | `packages/contract/src/financials.ts` (flat single-period `FinancialStatements`; `MarketDataProvider` has `getPrice/getSharesOutstanding/getMarketCap/getDividendYield`; `Exchange`, `CompanyRef`, `MarketData`, `PeerData` all missing) | Replace the file with the Part 4 `financials.ts` verbatim. Ripple: adapter, providers, app, tests. |
| 4 | `contract/extraction.ts`: `ExtractionLine`, `ExtractionResult` | OK | `packages/contract/src/extraction.ts` | None (matches Part 4). |
| 5 | Vite `base` set (`'/'` root vs `'/valuation-bot/'` project page); build script; precomputed `FinancialStatements` JSON in static assets fetched by the app; `DEPLOY.md` describing both options; do not push Pages yet | Missing | `packages/valuation-creator/app/vite.config.ts` (no `base`); no static-JSON fetch path; `DEPLOY.md` absent | Add configurable `base`; add a script that writes adapter output JSON into `app/public/`; have the app `fetch` it via the provider; write `DEPLOY.md`. |
| 6.1–6.5 | `FixtureProvider` populated with `SIA_FINANCIALS`, `SIA_MARKET`, `SIA_ASSUMPTIONS`, `SIA_FORECAST_DRIVERS`, `SIA_BETA_PEERS`, `SIA_COMPS_PEERS` | Missing | `packages/valuation-creator/app/src/fixtures/siaData.ts` (every export is an empty TODO stub array) | Populate all six datasets verbatim from masterprompt2.md §6.1–6.5 in the new `providers/FixtureProvider.ts`. |
| 6.3/6.7 | Base assumptions drive the reference outputs (Ke 7.35%, after-tax Kd 3.4%, WACC 6.1%, beta 0.6868, avg asset beta 0.4805, ΣPV FCFF 1948.25, Gordon $9.8725, exit $5.9055 / −9.0%, DDM $3.8674, comps ranges, 52-week 6.21/6.92/7.63) | Missing / Wrong | `core/__tests__/fixtures/siaValuation.ts`, `siaForecast.ts` (invented data with reverse-calibrated constants — `netDebt: 25398.923`, `specialDPS 0.18809` — solved to hit 9.8725/5.9055; **5.9055 is mislabelled as the DDM value**; none of the other §6.7 numbers are asserted) | Replace the invented fixtures with the Part 6 data; assert every §6.7 reference number (tolerance ±0.5% or ±$0.02); remove calibration constants. |
| 6.6 | Sensitivity grid axes: DCF Gordon WACC [4.1%..8.1%] × TGR [0.5%..2.5%]; exit WACC × margin [20.38%..24.38%] (option: exit multiple [2.7454..6.7454]); DDM COE [5.35%..9.35%] × TGR [0.5%..2.5%] | Missing | `App.tsx` (grids centered on computed WACC with non-spec steps); no DDM grid | Center grids per §6.6 with spec steps; add the DDM grid. |
| 6.8 | Tier 1: feed documented WACC inputs + `SIA_FORECAST_DRIVERS` FCFF + DDM inputs + peer/comps tables; assert every §6.7 number to the cent | Wrong | `core/__tests__/exactMath.test.ts` (runs on invented fixtures; asserts only 2 numbers, one mislabelled) | Rewrite Tier 1 per §6.8 against the Part 6 fixture and full §6.7 reference set. |
| 6.8 | Tier 2: feed `SIA_FINANCIALS` + `SIA_ASSUMPTIONS`; reproduce `SIA_FORECAST_DRIVERS` (EBIT/D&A/ΔNWC/EBITDA) within tolerance; document the WC-days convention | Wrong | `core/__tests__/forecast.test.ts` + `fixtures/siaForecast.ts` (invented drivers/schedules, EBITDA-margin model) | Rewrite Tier 2 on the real fixture once the forecast layer matches Part 2.2; calibrate + document the days convention. |
| 6.8 | Adapter round-trip: hand-built `ExtractionResult[]` for SIA → adapter → equals `SIA_FINANCIALS` | Wrong | `core/__tests__/adapterRoundTrip.test.ts` + `fixtures/siaExtraction.ts` (invented mini-statements; asserts the old flat shape; lives in `core` and imports adapter by relative path) | Rebuild the fixture to reproduce `SIA_FINANCIALS` exactly; move the test into `packages/adapter`. |
| 7 | Initial commit pushed to `keith2929/valuation-bot` | Missing | repo root | After all fixes: `git init && git add . && git commit -m "Initial commit: SGX extraction + valuation monorepo with SIA fixture" && git branch -M main && git remote add origin https://github.com/keith2929/valuation-bot.git && git push -u origin main`; stop and report if push auth fails. |
